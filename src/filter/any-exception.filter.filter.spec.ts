import { AllExceptionsFilter } from './any-exception.filter.filter';

describe('AnyException.FilterFilter', () => {
  it('should be defined', () => {
    expect(new AllExceptionsFilter()).toBeDefined();
  });
});
